# tpch

Generated by Schemalytics.

## Structure
- **bronze/**: Raw passthrough from source (8 models)
- **silver/dimensions/**: Dimensional models (6 models)
- **silver/facts/**: Fact tables (2 models)
- **gold/**: Pre-aggregated metrics (3 models)

## Semantic Layer
See `semantic_models.yml` for LLM-ready metadata including:
- Available metrics and their definitions
- Dimensional model structure
- Query patterns and guidelines

## Models

### Dimensions
- dim_customer: customer
- dim_nation: nation
- dim_part: part
- dim_partsupp: partsupp
- dim_region: region
- dim_supplier: supplier

### Facts
- fct_lineitem: one row per line item
- fct_orders: one row per order

### Gold Aggregates
- agg_daily_lineitem_quantity (daily): Daily aggregates for line items by part and supplier
- agg_monthly_lineitem_revenue (monthly): Monthly aggregates for line items by part and supplier
- agg_daily_orders_totalprice (daily): Daily aggregates for orders by customer

## Usage with LLMs
The semantic layer provides structured metadata for LLM-powered analytics:
1. LLMs can read `semantic_models.yml` to understand available metrics
2. Pre-aggregated Gold models provide fast query performance
3. Clear grain and dimension definitions help LLMs generate correct queries

Example LLM prompt:
```
Using the semantic layer in semantic_models.yml, write a SQL query to analyze
daily revenue trends over the last 30 days.
```
