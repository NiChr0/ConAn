name: 'tpch'
version: '1.0.0'

profile: 'tpch'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  tpch:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table